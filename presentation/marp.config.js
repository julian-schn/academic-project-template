// Marp CLI configuration
// Shared by the dev server, build, and export scripts (package.json).
// https://github.com/marp-team/marp-cli#configuration-file
export default {
  // Allow raw HTML in slides (needed for any custom markup you add later).
  html: true,
  // Register the custom theme(s) in ./themes (e.g. the "hdm" theme).
  themeSet: './themes',
};
